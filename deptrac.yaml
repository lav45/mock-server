deptrac:
  paths:
    - ./src
    - ./vendor

  layers:
    - name: Bootstrap
      collectors:
        - type: classNameRegex
          value: ~^Lav45\\MockServer\\Bootstrap\\.*~

    - name: DataFactory
      collectors:
        - type: classNameRegex
          value: ~^Lav45\\MockServer\\DataFactory\\.*~

    - name: Domain
      collectors:
        - type: classNameRegex
          value: ~^Lav45\\MockServer\\Domain\\.*~

    - name: Driver
      collectors:
        - type: classNameRegex
          value: ~^Lav45\\MockServer\\Driver\\.*~

    - name: Engine
      collectors:
        - type: classNameRegex
          value: ~^Lav45\\MockServer\\Engine\\.*~

    - name: Middleware
      collectors:
        - type: classNameRegex
          value: ~^Lav45\\MockServer\\Middleware\\.*~

    - name: Parser
      collectors:
        - type: classNameRegex
          value: ~^Lav45\\MockServer\\Parser\\.*~

    - name: Responder
      collectors:
        - type: classNameRegex
          value: ~^Lav45\\MockServer\\Responder\\.*~

    - name: Vendor
      collectors:
        - type: directory
          value: ./vendor

  ruleset:
    Bootstrap:
      - DataFactory
      - Domain
      - Engine
      - Middleware
      - Parser
      - Responder
      - Vendor
    Middleware:
      - DataFactory
      - Responder
      - Parser
      - Domain
      - Engine
      - Vendor
    DataFactory:
      - Domain
      - Engine
      - Parser
      - Vendor
    Parser:
      - Vendor
    Responder:
      - Domain
      - Engine
      - Vendor
    Engine:
      - Domain
    Driver:
      - Domain
      - Engine
      - Vendor