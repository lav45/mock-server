[
    {
        "env": {
            "webhook": {
                "token": "e71ad173-dacf-493c-be55-643074fdf41c",
                "url": "{env.WEBHOOK_CATCHER_URL}/storage"
            },
            "uuid": "{{faker.uuid}}",
            "correlationId": "{{faker.numerify('############')}}"
        },
        "request": {
            "method": "POST",
            "url": "/webhook/{id}"
        },
        "webhooks": [
            {
                "url": "{{env.webhook.url}}"
            },
            {
                "delay": 0.5,
                "url": "{{env.webhook.url}}",
                "headers": {
                    "Content-Type": "application/json"
                },
                "text": "{\"text\": \"Hello world\"}"
            },
            {
                "method": "POST",
                "url": "{env.WEBHOOK_CATCHER_URL}/storage?id=300",
                "headers": {
                    "x-api-token": "e71ad173-dacf-493c-be55-643074fdf41c"
                },
                "json": {
                    "ID1": "ID: {request.get.id}",
                    "ID2": "ID: {{request.get.id}}",
                    "ID3": "ID: {request.post.id}",
                    "ID4": "ID: {{request.post.id}}",
                    "get": "{{request.get}}",
                    "post": "{{request.post}}",
                    "urlParams": "{{request.urlParams}}",
                    "urlParamsId": "{{request.urlParams.id}}"
                }
            },
            {
                "method": "PUT",
                "url": "{{env.webhook.url}}",
                "headers": {
                    "x-api-token": "e71ad173-dacf-493c-be55-643074fdf41c"
                },
                "json": [
                    {
                        "id": "{{faker.uuid}}",
                        "name": "{{faker.name}}"
                    },
                    {
                        "id": "{{faker.uuid}}",
                        "name": "{{faker.name}}"
                    },
                    {
                        "id": "{{faker.uuid}}",
                        "name": "{{faker.name}}"
                    },
                    {
                        "id": "{{faker.uuid}}",
                        "name": "{{faker.name}}"
                    }
                ]
            },
            {
                "method": "GET",
                "url": "{env.WEBHOOK_CATCHER_URL}/storage?sss=get",
                "headers": {
                    "x-api-token": "e71ad173-dacf-493c-be55-643074fdf41c"
                }
            },
            {
                "method": "DELETE",
                "url": "{{env.webhook.url}}",
                "headers": {
                    "x-api-token": "e71ad173-dacf-493c-be55-643074fdf41c"
                }
            },
            {
                "method": "POST",
                "url": "{{env.webhook.url}}",
                "headers": {
                    "Content-Type": "application/x-www-form-urlencoded"
                },
                "text": "name=John&age=12"
            },
            {
                "method": "POST",
                "url": "{{env.webhook.url}}",
                "headers": {
                    "Content-Type": "multipart/form-data; boundary=FB"
                },
                "text": "--FB\r\nContent-Disposition: form-data; name=\"name\"\r\n\r\nJohn\r\n--FB\r\nContent-Disposition: form-data; name=\"age\"\r\n\r\n12\r\n--FB--\r\n"
            },
            {
                "url": "{{env.webhook.url}}",
                "json": {
                    "id": "{{faker.bothify('TEST####')}}",
                    "correlationId": "{{env.correlationId}}",
                    "uuid": "{{env.uuid}}"
                },
                "headers": {
                    "X-API-Token": "{{env.webhook.token}}"
                }
            },
            {
                "url": "{{env.webhook.url}}",
                "headers": {
                    "Content-Type": "application/json"
                },
                "text": "{\"text\": \"OK\"}"
            }
        ]
    }
]